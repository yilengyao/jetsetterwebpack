doctype html
html(lang='en')
    head 
        title Jetsetter 
        meta(charset='UTF-8')
        meta(name='viewport', content='width=device-width, initial-scale=1')
        // Add this CSP meta tag that allows inline styles and proper WebSocket
        meta(http-equiv="Content-Security-Policy", content="default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-eval'; connect-src 'self' ws://localhost:8080 http://localhost:8080")
        link(rel='stylesheet', href='style.css')
    body
        #application 
            .loading Loading things  no
        script. 
            require('./renderer')